'use strict';

class Sanitizer {

    constructor(parameters) {
        this.parameters = parameters;
    }

    /**
     * Sanitize the given field
     * @param {Field} field
     */
    sanitize(field) {

    }
}

module.exports = Sanitizer;